syntax = "proto3";

package wallet;

service Wallet {
    rpc GetTotal (Total) returns (Total) {
    };
    rpc AddOperation (Operation) returns (Total) {
    };
}

message Total {
    int64 value = 1;
}

message Operation {
    int64 value = 1;
    string note = 2;
}